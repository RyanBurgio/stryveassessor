import React from 'react';
import ReactDOM from 'react-dom/client';
import StryveValueModel from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StryveValueModel />
  </React.StrictMode>
);
